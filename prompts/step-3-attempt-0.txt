# Step 3/8: Implement mobile header (P1)
# Mode: fresh
# Timestamp: 2026-02-27T21:03:51Z
# ─────────────────────────────────────────

You are building Step 3 of 8 for `/Users/jarivs/Documents/JarvisProjects/kanban-board`.

Context (what came before)
- Step 1 (done): repo discovery + baseline snapshots.
- Step 2 (done): introduced an AppShell layout contract that owns responsive orchestration.
  - `src/client/components/AppShell.jsx` exports `AppShell({ sidebar, header, isSidebarOpen, onSidebarClose, children })`
  - It renders a fixed left drawer container with `lg:` static behavior, a backdrop close button (`lg:hidden`), ESC-to-close via `useEffect`, and a `min-w-0` main content region.

Step 3 goal (P1): Implement the mobile header
- On mobile breakpoints (the repo uses Tailwind `lg` breakpoint heavily: `lg:hidden` / `hidden lg:block`), show a compact header that includes:
  - Hamburger button (touch target ≥ 44px) to open the project drawer (`setIsSidebarOpen(true)`)
  - Project name + status (compact, truncates safely)
  - Activity log button (touch target ≥ 44px) to toggle the existing `ActivityLog` panel

Acceptance criteria
- On an iPhone-sized viewport (390×844), the header fits without clipping; buttons are comfortably tappable.
- Desktop header/layout remains unchanged (mobile header hidden on desktop).

Files to create / modify (use these exact paths)
1) Create: `src/client/components/MobileHeader.jsx`
2) Modify: `src/client/App.jsx`
3) Modify (required so the pipeline can run `npm run build`): `package.json`

Implementation details (must match existing code patterns and state)
A) New component: `src/client/components/MobileHeader.jsx`
- Export a default React component (function component) consistent with the repo’s style (plain `.jsx`, Tailwind classes, no TS).
- Suggested signature (keep it simple and explicit):
  - `export default function MobileHeader({ project, boardId, isActivityOpen, onOpenSidebar, onToggleActivity }) { ... }`
- Render a single-row header bar:
  - Left: hamburger button
    - `type="button"`
    - `aria-label="Open project drawer"`
    - Touch target: use `h-11 w-11` (44px) and `inline-flex items-center justify-center`
  - Middle: project identity block
    - `min-w-0 flex-1` so long names don’t overflow
    - Project name: `truncate`, small readable typography (e.g. `text-sm font-semibold`)
    - Status: compact pill (similar to `src/client/components/ProjectHeader.jsx` and `src/client/components/Sidebar.jsx` status patterns)
      - Project statuses used in the app: `active`, `completed`, `archived`
      - Add a small local mapping like `statusClasses` to style the pill; fall back to archived styling on unknown.
  - Right: Activity button
    - `type="button"`
    - `aria-label` like `"Toggle activity log"`
    - Touch target: use `h-11` plus horizontal padding (or `h-11 w-11` if icon-only)
    - Disable when `!boardId` (mirrors existing desktop button `disabled={!board}` in `src/client/App.jsx`)
    - Label can be `Activity` (or `Close` when `isActivityOpen` is true), but keep it compact.
- Keep styling aligned with the dark theme already used (gray-950/900/800, gray borders, focus rings like other buttons).
- No new dependencies (no icon library). If you want icons, use small inline SVG.

B) Integrate into the shell header: `src/client/App.jsx`
- Currently `AppShell` is invoked with an inline `header={ (...) }` that includes:
  - A mobile-only “Projects” button (`lg:hidden`) that opens the sidebar
  - Desktop-visible action buttons: “Plan”, “Logs”, “Activity”
- Replace that arrangement with a true mobile header + existing desktop header, using responsive wrappers:
  - Import: `import MobileHeader from './components/MobileHeader.jsx';`
  - `header={(
      <div>
        <div className="lg:hidden ..."> <MobileHeader ... /> </div>
        <div className="hidden lg:block px-6 pb-0 pt-6 lg:px-8 lg:pt-8"> ...existing desktop actions... </div>
      </div>
    )}`
- Mobile header props must wire to the existing state in `App.jsx`:
  - `project={selectedProject}`
  - `boardId={board?.id}` (or pass `board?.id` as `boardId`)
  - `isActivityOpen={isActivityOpen}`
  - `onOpenSidebar={() => setIsSidebarOpen(true)}`
  - `onToggleActivity={() => { setIsActivityOpen((prev) => !prev); setIsLogsOpen(false); setIsDocsOpen(false); setIsSidebarOpen(false); }}`
- Desktop header actions must remain behaviorally identical to the current code:
  - “Plan” button toggles `isDocsOpen`, closes logs/activity/sidebar
  - “Logs” button toggles `isLogsOpen`, closes docs/activity/sidebar
  - “Activity” button toggles `isActivityOpen`, closes logs/docs/sidebar
  - Keep the existing Tailwind classes for these buttons so the desktop look stays the same.
- Remove the old mobile “Projects” button from the desktop header block (it becomes redundant once `MobileHeader` exists).

C) Ensure `npm run build` exists and works: `package.json`
- Add a `build` script that builds the frontend using the existing Vite config:
  - `"build": "vite build --config vite.config.js"`
- Do not add scripts that would be run in CI and block forever (don’t encourage `dev`/`start` in the execution steps).

Dependencies
- No new packages needed.
- Use existing React + Tailwind + Vite setup already in `package.json` / `vite.config.js`.

Testing / validation (match repo patterns)
- Run unit/integration tests: `npm test`
- Run build: `npm run build`
- Do NOT run `npm run dev` or `npm start`.

Shell command hygiene
- When running shell commands that include file paths, ALWAYS quote file paths (zsh treats `[...]` as globs).

Mandatory rules (include verbatim)
- YOU ARE RUNNING NON-INTERACTIVELY. There is no human to answer questions.
- NEVER ask for confirmation. NEVER ask clarifying questions. NEVER wait for input.
- If you encounter unexpected state (dirty files, missing deps, etc), handle it yourself and continue.
- Ignore any files you didn't create — they are orchestration artifacts.
- ALL existing tests must still pass
- npm run build must succeed
- Do NOT run npm run dev or npm start — they block forever
- CRITICAL: Running npm run dev or npm start WILL hang your session indefinitely. Never do it.
- IMPORTANT: When using shell commands, ALWAYS quote file paths (especially Next.js route segments like app/api/foo/[id]/route.ts) because zsh treats [id] as a glob.

# Step 1/2: Fix Tailwind v4 + Core Layout
# Mode: resume
# Timestamp: 2026-02-26T03:02:24Z
# ─────────────────────────────────────────

You were working on Step 1 of 2 for kanban-board UI revamp and got interrupted. You already completed:
- ✅ src/client/styles/index.css — Tailwind v4 syntax with @import "tailwindcss" and @theme block
- ✅ src/client/App.jsx — Flexbox layout with sidebar w-64 + main flex-1

Remaining deliverables (finish these):
1. Delete tailwind.config.js (not needed for Tailwind v4 with vite plugin)
2. Revamp Sidebar.jsx: dark bg-gray-950, proper padding, project items with hover states, status dots (green/blue/gray), "New Project" button styled as ghost button
3. Revamp CreateProjectModal.jsx: centered modal with backdrop blur/dim, proper input styling, rounded buttons

Read DESIGN-v3.md for full visual spec.

When done:
1. Run the test suite and make sure ALL 19 tests pass
2. Run `npx vite build` and make sure the production build succeeds

You are building Step 6 of 6 for Qanat: Kanban Board Auth & Multi-Tenancy.
Read DESIGN.md for the full architecture.

Previous steps completed: Database Schema & Auth Utilities, Auth Middleware & Login API, Invite System API, Change Password Flow, Login UI & Auth Context
Build on the existing code. Do not break what already works.

## Your task: Admin User Management UI

Deliverables:
- AdminUsersPage.tsx — table of users with role/status
- Ability to change user role (admin/member)
- Ability to disable/enable users
- Invite creation form (email + role)
- Link to admin page in sidebar (admin only)
- Seed script updates: ensure admin user exists on fresh DB

Acceptance criteria:
- Admin sees user management in sidebar
- Members don't see admin link
- Admin can invite, change roles, disable users
- All actions reflected immediately in UI
- Existing board/card/project functionality unbroken

## Rules (mandatory)

YOU ARE RUNNING NON-INTERACTIVELY. There is no human to answer questions.
NEVER ask for confirmation. NEVER ask clarifying questions. NEVER wait for input.
If you encounter unexpected state (dirty files, missing deps, etc), handle it yourself and continue.
Ignore any files you didn't create — they are orchestration artifacts.

When done:
1. Run the test suite — ALL tests must pass (existing + new)
2. Run `npm run build` (or equivalent) — must succeed with zero errors
3. Do NOT run long-lived processes like `npm run dev` or `npm start` — they block forever and will kill your session
4. Do NOT start dev servers for verification — use `npm run build` to verify compilation
5. If you need to check that a server works, use `npm run build` only

CRITICAL: Running `npm run dev` or `npm start` WILL hang your session indefinitely. Never do it.

Responsive requirement:
- Follow DESIGN.md section: 'Responsive / Mobile Layout'
- If you touch UI/layout, ensure it remains usable on mobile widths (e.g., 390px). Avoid overlap/clipping.
- Prefer responsive Tailwind classes (sm/md/lg) over fixed widths.

Feature requirements:
- Implement ALL items in DESIGN.md 'Feature Checklist' (MUST scope). Do not silently skip features.
- Do NOT implement SHOULD/LATER scope unless a MUST item depends on it.
- Follow DESIGN.md 'Assumptions' + 'User Flows' — do not underbuild just because the brief is short.

UI / UX quality bar:
- Follow DESIGN.md section: 'UI / UX Quality Bar' (or write N/A in design for non-UI projects).
- Default to a modern component kit (shadcn/ui + lucide-react) unless the design forbids it.
- Ship professional states: hover/focus/disabled, empty states, loading states, error messaging.
- Ensure typography + spacing feel deliberate (not raw HTML defaults).
- Accessibility: labels for inputs, aria-labels for icon-only buttons, visible focus.

If you believe a checklist item is impossible within this step: still implement the best partial, and leave a TODO in code + log it in a test or comment — never silently omit.